name: Weather Data CICD

on:
  push:
    branches:
      - main

jobs:
  deploy-codebase:
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest

    steps:
      # Checkout the repository
      - name: Checkout Code
        uses: actions/checkout@v3

      # Authenticate to GCP Account
      - name: Authenticate to GCP
        uses: google-github-actions/auth@v1
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      # Setup Google Cloud SDK
      - name: Setup Google Cloud SDK
        uses: google-github-actions/setup-gcloud@v1
        with:
          project_id: ${{ secrets.GCP_PROJECT_ID }}

      # Sync Spark job to GCS
      - name: Upload Spark Job to GCS
        run: |
          gsutil cp weather_data_pipeline/spark_job/weather_data_processing.py gs://weather-data-mid/script/

      # Sync Airflow DAGs to Composer
      - name: Upload Airflow DAGs to Composer
        run: |
          gcloud composer environments storage dags import \
            --environment airflow-qa \
            --location us-central1 \
            --source weather_data_pipeline/dags
